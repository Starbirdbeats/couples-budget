# Supabase project credentials (publishable — anon key is safe in client bundles;
# row-level security is the data boundary). Copy to .env and fill in.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
